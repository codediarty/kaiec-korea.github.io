# 한국AI윤리협회 홈페이지 — 배포 및 운영 가이드

유지비 **0원**으로 홈페이지를 올리고, **네이버·구글 검색에 노출**시키는 전체 과정입니다.
순서대로 따라 하시면 됩니다. 개발 지식은 필요 없습니다.

---

## 요약

| 항목 | 내용 | 비용 |
|---|---|---|
| 호스팅 | GitHub Pages | **0원 (영구 무료)** |
| SSL(https) | 자동 적용 | **0원** |
| 트래픽 | 월 100GB까지 | **0원** |
| 주소 | `아이디.github.io` | **0원** |
| 검색 노출 | 네이버 서치어드바이저 · 구글 서치콘솔 | **0원** |
| 문의 폼 | Formspree 무료 플랜 (월 50건) | **0원** |
| (선택) 도메인 | `.or.kr` 등 직접 구매 시 | 연 1~2만원 |

---

> **✅ 이 사이트의 확정 주소: `https://kaiec.kr`**
> 모든 파일(canonical, sitemap.xml, rss.xml, robots.txt)에 이 주소가 이미 반영되어 있습니다.
> 커스텀 도메인(kaiec.kr)이 CNAME 파일로 함께 들어 있어, 전체 업로드해도 도메인 설정이 유지됩니다.

## 1단계 — GitHub 가입 (3분)

1. https://github.com 접속 → **Sign up**
2. 이메일 / 비밀번호 / 아이디 입력
   - **아이디는 반드시 `kaiec-korea`** 로 만드세요.
3. 이메일 인증 완료

---

## 2단계 — 저장소(Repository) 만들기 (2분)

1. 로그인 후 오른쪽 위 **+** → **New repository**
2. **Repository name** 에 `kaiec-korea.github.io` 를 정확히 입력
   - ⚠️ 이 이름이어야 주소가 깔끔하게 나옵니다.
3. **Public** 선택 (필수 — Private은 무료 Pages가 안 됩니다)
4. **Create repository** 클릭

---

## 3단계 — 파일 올리기 (5분)

1. 만든 저장소 화면에서 **uploading an existing file** 링크 클릭
   (또는 **Add file** → **Upload files**)
2. 압축을 푼 폴더 안의 **내용물 전체**를 드래그해서 놓습니다.
   - ⚠️ `kaiec-site` **폴더째로** 올리면 안 됩니다. 폴더를 열고 **안에 있는 파일들**을 올리세요.
   - 올려야 할 것: `index.html`, `about.html` … 모든 html 파일 + `assets` 폴더 + `robots.txt` + `sitemap.xml` + `404.html` + `.nojekyll`
   - 올릴 필요 없는 것: `build.py`, `icons.py`, `배포-가이드.md` (올려도 무방합니다)
3. 아래 **Commit changes** 버튼 클릭

---

## 4단계 — GitHub Pages 켜기 (1분)

1. 저장소 상단 **Settings** → 왼쪽 메뉴 **Pages**
2. **Source** 를 `Deploy from a branch` 로 두고
   **Branch** 를 `main` / `/(root)` 으로 선택 → **Save**
3. 1~2분 뒤 새로고침하면 상단에 주소가 나옵니다.
   → `https://아이디.github.io`

**여기까지 하면 사이트가 인터넷에 올라간 상태입니다.** 접속해서 확인해 보세요.

---

## 5단계 — 사이트 주소 반영하기 → ✅ 이미 완료됨

모든 파일에 확정 주소 `https://kaiec.kr` 가 이미 반영되어 있으므로
**이 단계는 건너뛰면 됩니다.**

(참고: 나중에 주소를 바꾸게 되면 `build.py` 상단의 `SITE_URL` 과 `robots.txt` 마지막 줄만
고치고 `python3 build.py` 를 다시 실행하면 전체에 반영됩니다.)

---

## 6단계 — 네이버 검색에 노출시키기 (10분) ★ 핵심

네이버는 알아서 크롤링하지 않습니다. **직접 등록해야만** 검색됩니다.
(다른 협회 사이트들이 네이버에 나오는 것도 전부 이 과정을 거친 것입니다.)

1. https://searchadvisor.naver.com 접속 → 네이버 계정으로 로그인
2. **웹마스터 도구** → 사이트 주소 입력 (`https://아이디.github.io`) → 등록
3. **사이트 소유확인** 화면에서 **HTML 태그** 방식 선택
   - `<meta name="naver-site-verification" content="abc123...">` 형태의 코드가 나옵니다.
   - `content="..."` 안의 **값만** 복사합니다.
4. GitHub에서 **모든 html 파일**을 열어 아래 줄을 찾아 값을 붙여넣습니다.
   ```html
   <meta name="naver-site-verification" content="">
                                                 ↑ 여기에 붙여넣기
   ```
   (최소한 `index.html` 하나만 해도 소유확인은 통과합니다.)
5. 서치어드바이저로 돌아가 **소유확인** 클릭
6. 확인되면 다음 3가지를 꼭 하세요:
   - **요청 → 사이트맵 제출** : `sitemap.xml` 입력
   - **요청 → RSS 제출** : `rss.xml` 입력 (소식 게시글이 자동으로 네이버에 전달됩니다)
   - **요청 → 웹페이지 수집** : 각 페이지 주소를 하나씩 넣어 수집 요청
   - **검증 → robots.txt** : 정상인지 확인

> 등록 후 실제 검색 노출까지는 보통 **3일 ~ 2주** 걸립니다. 바로 안 나온다고 잘못된 게 아닙니다.

---

## 7단계 — 구글 검색에 노출시키기 (5분)

1. https://search.google.com/search-console 접속 → 로그인
2. **URL 접두어** 방식으로 사이트 주소 입력
3. 소유확인 → **HTML 태그** 방식 선택 → 나온 값을
   `<meta name="google-site-verification" content="">` 에 붙여넣기
4. **Sitemaps** 메뉴에서 `sitemap.xml` 제출

구글은 네이버보다 빠릅니다. 보통 1~3일이면 노출됩니다.

---

## 8단계 — 폼 동작 확인 (설정할 것 없음)

두 가지 접수 창구가 **이미 연결되어 있습니다.** 별도 가입이 필요 없습니다.

**① 위원 지원서 (`apply.html`)** — 구글폼이 페이지 안에 삽입되어 있습니다.
- 응답은 기존처럼 구글폼 관리 화면에서 확인하시면 됩니다.
- 지원서 구글폼을 바꾸고 싶으면 `build.py` 상단의 `GOOGLE_FORM` 주소만 교체하고
  `python3 build.py` 를 다시 실행하세요. (또는 `apply.html`에서 폼 주소 2곳을 직접 교체)

**② 제휴·MOU 문의 (`mou.html`) / 강의 신청 (`lecture.html`)** — 방문자가 양식을 작성하고
버튼을 누르면 **본인 메일 앱이 열리면서 내용이 자동으로 담겨** 협회 메일로 발송됩니다.
서버가 필요 없어 유지비 0원이 유지됩니다. 받는 주소를 바꾸려면 `build.py`의 `EMAIL`을 수정 후 재빌드하세요.

---

## 소식(게시글) 올리기 — 검색 노출의 핵심 ★

글 1개 = 독립된 HTML 페이지 1개로 만들어지기 때문에, 글을 쓸수록
네이버·구글에 잡히는 페이지가 늘어납니다. **키워드를 노린 글을 꾸준히 올리는 것**이
이 사이트 SEO의 핵심 전략입니다.

1. `posts-src/` 폴더에 `.md` 파일을 만듭니다. **파일명이 곧 주소**가 됩니다.
   예) `2026-09-01-ai-report.md` → `post-2026-09-01-ai-report.html`
2. 아래 양식으로 작성합니다.
   ```
   제목: 글 제목
   날짜: 2026.09.01
   분류: 캠페인          (공지/캠페인/활동/연구·정책 중 하나)
   키워드: AI윤리, 표절검사, 카피클린   (검색에 잡히고 싶은 단어)
   이미지: banner.png    (assets/img/posts/에 넣은 파일명. 없으면 비움)
   요약: 목록·검색결과에 보이는 한두 문장
   ---
   본문. 빈 줄로 문단 구분. ## 소제목, - 목록, **굵게**, [링크](주소),
   ![설명](사진파일명) 문법을 쓸 수 있습니다.
   ```
3. `python3 build.py` 실행 → 게시글 페이지 + 목록 + 홈 최근소식 + sitemap.xml + rss.xml 이 전부 자동 갱신됩니다.
4. 바뀐 파일을 GitHub에 업로드합니다.
5. (권장) 서치어드바이저 → 웹페이지 수집에 새 글 주소를 넣으면 노출이 빨라집니다.

자세한 예시는 `posts-src/_작성방법.txt` 와 이미 들어 있는 샘플 글 3개를 참고하세요.
파이썬 실행이 어려우면 Claude에게 글 내용을 주고 "게시글로 만들어서 빌드해줘"라고 하셔도 됩니다.

## 내용 수정하기

### 자주 바뀌는 내용 — HTML을 건드릴 필요 없음

| 수정할 내용 | 방법 |
|---|---|
| 소식·캠페인 글 추가 | `posts-src/`에 .md 추가 → `python3 build.py` (바로 위 섹션 참고) |
| 위원 명단 추가·수정 | `assets/js/members-data.js` 수정 (재빌드 불필요) |
| 제휴 기관 로고 추가 | `assets/js/partners-data.js` 수정 (재빌드 불필요) |
| 연혁 추가 | `assets/js/history-data.js` 수정 (재빌드 불필요) |

js 파일들은 맨 위에 작성 방법이 주석으로 적혀 있고,
GitHub에서 파일 → 연필 아이콘 → 수정 → **Commit changes** 하면 1분 내 반영됩니다.

### 로고 이미지 넣기
`assets/img/` 폴더에 이미지 파일을 올린 뒤 `partners-data.js` 에 파일명을 적으면 됩니다.

### 메뉴·본문 수정 (다시 빌드)
`build.py` 안에 모든 본문 내용이 들어 있습니다. 수정 후 아래를 실행하면 9개 HTML이 다시 만들어집니다.

```bash
python3 build.py
```

파이썬이 없다면 HTML 파일을 직접 수정해도 됩니다.
(단, 메뉴/푸터를 바꿀 땐 9개 파일을 모두 고쳐야 합니다.)

---

## (선택) 도메인 연결하기 — 연 1~2만원

`kaiec.kr` 대신 `kaiec.or.kr` 같은 주소를 쓰고 싶다면:

1. 가비아·후이즈·호스팅케이알 등에서 도메인 구매
   - ⚠️ **`.or.kr` 은 비영리 단체만 등록 가능**합니다. 고유번호증 등 단체 증빙 서류가 필요하고,
     개인·영리법인은 등록할 수 없습니다. 어려우면 `.kr` / `.com` / `.org` 를 쓰셔도 됩니다.
2. 도메인 관리 페이지에서 DNS 레코드 설정
   ```
   A     @     185.199.108.153
   A     @     185.199.109.153
   A     @     185.199.110.153
   A     @     185.199.111.153
   CNAME www   아이디.github.io
   ```
3. GitHub 저장소 → **Settings → Pages → Custom domain** 에 도메인 입력 → Save
4. **Enforce HTTPS** 체크 (몇 분 뒤 활성화됨)
5. 도메인이 바뀌었으므로 `robots.txt`, `sitemap.xml`, 서치어드바이저 등록을 새 주소로 다시 해주세요.

**호스팅 비용은 여전히 0원입니다.** 도메인 값만 듭니다.

---

## 파일 구조

```
kaiec-site/
├── index.html          홈
├── about.html          협회 소개
├── business.html       주요사업
├── members.html        조직·위원 (조직도 · 명단)
├── lecture.html        강의·교육 신청 (강사 파견)
├── partner.html        AI 윤리 파트너
├── copyclean.html      카피클린 제휴
├── news.html           커뮤니티 (게시판 목록)
├── post-*.html         게시글 페이지들 (build.py가 자동 생성)
├── mou.html            MOU·대외협력 (제휴 문의 폼)
├── apply.html          위원 지원 (구글폼 연동)
├── 404.html            없는 페이지 안내
├── robots.txt          검색로봇 안내
├── sitemap.xml         검색엔진용 페이지 목록 (자동 생성)
├── rss.xml             네이버 RSS 제출용 (자동 생성)
├── .nojekyll           GitHub Pages 설정 파일
├── build.py            페이지 생성 스크립트 (본문 내용 원본)
├── icons.py            아이콘 데이터 (수정 불필요)
├── posts-src/          ★ 소식 글 원본 (.md) — _작성방법.txt 참고
└── assets/
    ├── css/style.css   전체 디자인
    ├── js/
    │   ├── main.js            공통 동작
    │   ├── members-data.js    ★ 위원 명단
    │   ├── partners-data.js   ★ 제휴 기관
    │   └── history-data.js    ★ 연혁
    └── img/
        ├── favicon.svg   브라우저 탭 아이콘
        ├── og-image.png  카톡·SNS 공유 시 나오는 이미지
        └── posts/        ★ 게시글 이미지 넣는 곳
```

---

## 표현 관련 안내

현재 사이트 전체에 다음 원칙이 적용되어 있습니다. 내용을 수정하실 때도 유지해 주세요.

- 협회 성격은 **"AI 윤리 전문기관"**으로 표기했습니다. 정부기관·공공기관·국가공인을 주장하는 표현만 피하면 됩니다.
- 커뮤니티 칼럼은 "우회·통과 보장" 프레임 없이 **사전점검·표기·재작성** 프레임으로 작성되어 있습니다. 새 글도 이 톤을 유지하세요.
- 「카피클린」은 협회와 **협력하는 별도의 제휴 서비스**로 표기했고, 소속 관계가 아님을 명시했습니다.
- 아직 발생하지 않은 실적(회원 수, 협약 건수 등)은 숫자로 적지 않았습니다.
  실제 실적이 생기면 `news-data.js` 와 `history-data.js` 에 추가하세요.

---

## 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| 404 페이지가 뜬다 | 파일을 폴더째 올렸는지 확인 (`index.html`이 최상단에 있어야 함) |
| 디자인이 깨진다 | `assets` 폴더가 통째로 올라갔는지 확인 |
| 아이콘이 안 보인다 | 아이콘은 파일에 내장되어 있어 인터넷과 무관합니다. 캐시 삭제 후 재접속 |
| 지원서(구글폼)가 안 보인다 | 새 창 버튼으로는 열리는지 확인. 구글폼 설정에서 '응답 받기'가 켜져 있는지 확인 |
| 제휴 문의 버튼이 반응 없다 | 방문자 기기에 기본 메일 앱이 설정되어 있어야 합니다. 안내 문구의 메일 주소로 직접 발송 가능 |
| 네이버에 안 나온다 | 서치어드바이저 소유확인 → 사이트맵 제출 → 웹페이지 수집 요청까지 했는지 확인 (3일~2주 소요) |
